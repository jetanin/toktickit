# AI Use and Reflection (Lab 03)

I used **Antigravity IDE** (Google DeepMind) with models including **Claude Sonnet 4.6 (Thinking)** and **Gemini 3.8 Flash** as my main AI assistant throughout Lab 03, covering sprint specification drafting, API contract design, test planning, database schema design, UI specification, implementation, security auditing, and test refactoring.

## Selected Key Prompts

| Prompt Name                                                                    | Actual Prompt Text                                                                                                                                                                                                             | My Reflection                                                                                                                                                                                                                                                                                                                                  |
| :----------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Draft Sprint 3 Engineering Contract & Specifications**                       | เขียนไฟล์ specification.md, api-spec.md, ui-spec.md, tests.md ตามlabsheet Lab 3 อย่างละเอียด                                                                                                                                   | ได้โครงร่างและรายละเอียดข้อกำหนดครบทั้ง 4 ไฟล์ตามที่ Lab Sheet กำหนด ทั้งข้อกำหนดด้าน Functional Requirements (FR-01–FR-23), Business Rules (BR-01–BR-27), Data Model (`User`, `Ticket`, `PublicComment`, `InternalNote`), REST API Contracts, และ Test Matrix สำหรับ Lab 3 โดยตรง ทำให้ทีมมี Engineering Contract ที่ชัดเจนก่อนเริ่มเขียนโค้ด |
| **Schema Evolution & Data Migration (feature/2)**                              | Context: TokTickIT Lab 3, feature/2-user-migration. STEP 0 — Inspect first... STEP 1 — Evolve the schema: introduce the User model, keep existing records, backfill itPriority...                                              | ได้แผนการ migrate ข้อมูลจาก `DevelopmentRequester` เดิมไปสู่ `User` table ใหม่ โดยคง id และ email ไว้เพื่อให้ ticket requester foreign key ไม่ขาดตอน และสร้าง composite index `[ticketId, removedAt]` ตามข้อกำหนด 7.3 รวมทั้ง backfill ค่า `itPriority` เริ่มต้นให้สอดคล้องกับ `requestedPriority`                                             |
| **Audit Migration against Specification (Prompt B - Issue 2)**                 | ตรวจสอบ 3 ไฟล์ที่ส่งมาแล้ว โดยอ้างอิงกับ specification.md ที่ approve ไปก่อนหน้า (โดยเฉพาะหัวข้อ 7.2, 7.3, 7.4, 7.5)...                                                                                                        | การทำ audit prompt ทำให้ตรวจพบประเด็นสำคัญก่อน merge เช่น การขาด composite index `Attachment(ticketId, removedAt)` ในไฟล์ migration และการจัดการ password hash ใน seed data ทำให้แก้ปัญหาความปลอดภัยและความสมบูรณ์ของ schema ได้ทันท่วงที                                                                                                      |
| **Implement Authentication & Session Management (feature/3)**                  | Implement Issue 3: cookie-based authentication, POST /api/auth/login, GET /api/auth/me, POST /api/auth/change-password, POST /api/auth/logout, password complexity validator, mandatory password change gate.                  | AI ช่วยวางโครงสร้างระบบ session cookie (`HttpOnly`, `SameSite=Lax`) ร่วมกับ JWT token และ middleware ตรวจสอบ `mustChangePassword` เพื่อ gate การเข้าถึง operational endpoints พร้อมทั้งทำหน้า Login และ Change Password modal ที่ client พร้อม live complexity checklist                                                                       |
| **Audit Authentication Implementation against Contracts (Prompt B - Issue 3)** | Audit the work on feature/3-authentication against docs/lab-03/specification.md, api-spec.md, and tests.md. Report Pass/Fail with evidence per item.                                                                           | การตรวจสอบตามเกณฑ์ทำให้ยืนยันได้ว่า error response ของ login ไม่เปิดเผยความมีอยู่ของบัญชี (generic error "Invalid email or password" สำหรับทั้ง wrong password, unknown email, inactive account) และ password rules ถูก enforce ตาม BR-06 ครบถ้วน                                                                                              |
| **Branch & Commit Hygiene Correction**                                         | แก้สิ่งที่ push ไปบน branch lab3_staging อันล่าสุด โดยต้องอยู่บน branch feature/3-authentication เนื่องจาก push ผิด branch                                                                                                     | ได้เรียนรู้เรื่องการควบคุม branch และ git hygiene ในการทำงานร่วมกับ AI ที่ต้องระมัดระวังเรื่อง target branch ไม่ให้เกิด commit ปนเปื้อนใน staging branch ก่อนการ review และ merge อย่างเป็นทางการ                                                                                                                                              |
| **Test Suite Directory Consolidation (server/tests -> server/test)**           | ย้าย server\tests\lab-03\auth.api.test.ts ไปยัง server\test\lab-03 ให้มี auth.api.test.ts เพียงไฟล์เดียว แล้วทำให้ทุกอย่างใช้ได้                                                                                               | การจัดระเบียบโครงสร้างไดเรกทอรีทดสอบให้เป็นเอกภาพ (single source of truth) ตามมาตรฐานโปรเจกต์เดิมที่มี `server/test/lab-01` และ `server/test/lab-02` ป้องกันปัญหา vitest รันไฟล์ซ้ำซ้อน และอัปเดต documentation (`tests.md`) ให้ตรงกับความเป็นจริง                                                                                             |
| **Requester Regression & Public Comments Preparation (feature/4)**             | Context: TokTickIT Lab 3, feature/4-requester-regression. STEP 0 — Inspect first: Read docs/lab-03/specification.md BR-03 and AC-03... Report every place in current code where requesterId is still being read from client... | การ inspect โค้ดจริงก่อนเริ่ม implement ทำให้เห็นจุดที่ client ส่ง `X-Requester-Id` และเตรียม fallback สำหรับชุดทดสอบเก่าของ Lab 2 ทำให้การ migrate ไปใช้ authenticated session ไม่ทำให้ regression tests เดิมพัง                                                                                                                              |

---

## Reflection
